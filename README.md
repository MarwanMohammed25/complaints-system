# منظومة الشكاوى والمتابعة

نظام إدارة الشكاوى المبني على Firebase لتتبع ومعالجة شكاوى العملاء.

## المميزات

- ✅ تسجيل دخول آمن باستخدام Firebase Authentication
- 📝 إضافة شكاوى جديدة مع تفاصيل كاملة
- 📊 عرض وتتبع جميع الشكاوى
- 👥 إدارة المشرفين
- 🔍 بحث وتصفية متقدم
- 📱 تصميم متجاوب يعمل على جميع الأجهزة
- 🌐 واجهة عربية كاملة

## المتطلبات

- Node.js (الإصدار 14 أو أحدث)
- حساب Firebase

## التثبيت

### 1. تثبيت التبعيات

```powershell
cd complaints-system
npm install
```

### 2. إعداد Firebase

1. انتقل إلى [Firebase Console](https://console.firebase.google.com/)
2. قم بإنشاء مشروع جديد أو استخدم المشروع الموجود
3. قم بتفعيل:
   - **Authentication** (Email/Password)
   - **Realtime Database**
4. في إعدادات قاعدة البيانات، استخدم هذه القواعد للبدء:

```json
{
  "rules": {
    "complaints": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "supervisors": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```

### 3. إنشاء مستخدم للدخول

1. في Firebase Console، انتقل إلى **Authentication** > **Users**
2. اضغط على **Add User**
3. أدخل البريد الإلكتروني وكلمة المرور
4. احفظ البيانات لاستخدامها في تسجيل الدخول

## التشغيل

### تشغيل خادم محلي

```powershell
npm start
```

سيتم فتح التطبيق على `http://localhost:8080`

### أو استخدم أي خادم آخر

```powershell
# باستخدام Python
python -m http.server 8080

# باستخدام PHP
php -S localhost:8080
```

## البنية

```
complaints-system/
├── index.html              # صفحة تسجيل الدخول
├── firebase-config.js      # إعدادات Firebase
├── package.json           # التبعيات
├── css/
│   └── styles.css         # التنسيقات
├── js/
│   ├── login.js           # منطق تسجيل الدخول
│   ├── complaints.js      # منطق الشكاوى
│   ├── records.js         # منطق سجل الشكاوى
│   └── supervisors.js     # منطق المشرفين
└── pages/
    ├── complaints.html    # صفحة تقديم شكوى
    ├── records.html       # صفحة سجل الشكاوى
    └── supervisors.html   # صفحة المشرفين
```

## الاستخدام

### 1. تسجيل الدخول
- افتح التطبيق في المتصفح
- أدخل البريد الإلكتروني وكلمة المرور
- اضغط "دخول"

### 2. إضافة مشرف
- انتقل إلى صفحة "المشرفين"
- املأ البيانات المطلوبة (الاسم، الهاتف، المنطقة)
- اضغط "حفظ المشرف"

### 3. تقديم شكوى
- انتقل إلى صفحة "تقديم شكوى"
- املأ جميع الحقول المطلوبة
- اختر المشرف ونوع الشكوى
- اضغط "حفظ الشكوى"

### 4. عرض السجلات
- انتقل إلى صفحة "سجل الشكاوى"
- استخدم البحث والتصفية لإيجاد شكاوى محددة
- قم بتحديث حالة الشكاوى (قيد الانتظار، قيد المعالجة، تم الحل)

## الملاحظات الأمنية

⚠️ **تحذير**: ملف `firebase-config.js` يحتوي على مفاتيح Firebase. 

في الإنتاج:
- لا ترفع المفاتيح على GitHub
- استخدم متغيرات البيئة
- فعّل قواعد أمان Firebase بشكل صحيح

## دعم المتصفحات

- Chrome (موصى به)
- Firefox
- Safari
- Edge

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## الدعم

للمشاكل والاستفسارات، يرجى فتح issue في المشروع.
