# سياسة الأمان

## البيانات الحساسة

### مفاتيح Firebase
- **لا ترفع** ملف `firebase-config.js` على GitHub
- استخدم `.env` لتخزين المفاتيح
- قم بتفعيل قواعد الأمان في Firebase Console

### كلمات المرور
- يتم تشفيرها بواسطة Firebase Authentication
- لا تُخزن في قاعدة البيانات
- الحد الأدنى: 6 أحرف

## قواعد الأمان

### Firebase Realtime Database
```json
{
  "rules": {
    "appVersion": {
      ".read": true,
      ".write": "auth != null"
    },
    "complaints": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$complaintId": {
        ".validate": "newData.hasChildren(['customerName', 'phoneNumber', 'buildingNumber', 'area', 'supervisor', 'complaintType', 'complaintContent', 'status', 'createdAt'])"
      }
    },
    "supervisors": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$supervisorId": {
        ".validate": "newData.hasChildren(['name', 'phone', 'area', 'createdAt'])"
      }
    },
    "managers": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$managerId": {
        ".validate": "newData.hasChildren(['name', 'phone', 'area', 'createdAt'])"
      }
    },
    "documents": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}

### Content Security Policy
- تم تطبيق CSP على جميع الصفحات
- منع `unsafe-eval`
- السماح فقط بمصادر موثوقة

## الإبلاغ عن ثغرات أمنية

إذا اكتشفت ثغرة أمنية:
1. **لا تنشرها علناً**
2. تواصل مع المطورين مباشرة
3. قدم وصف مفصل للمشكلة

## أفضل الممارسات

### للمطورين:
- ✅ استخدم HTTPS دائماً
- ✅ فعّل webSecurity في Electron
- ✅ تحقق من المدخلات
- ✅ استخدم parameterized queries
- ❌ لا تستخدم `eval()` - **✅ غير مستخدم في التطبيق**
- ⚠️ localStorage للتخزين المؤقت فقط - **✅ يتم المزامنة مع Firebase**

### استراتيجية التخزين الحالية:
**نهج مزدوج آمن:**
- 🔐 **Firebase** - التخزين الأساسي (مُشفر، آمن، متزامن)
- ⚡ **localStorage** - التخزين المؤقت (للأداء السريع فقط)
- 🔄 **المزامنة التلقائية** - كل تغيير يُحفظ في Firebase أولاً

**البيانات المُخزنة:**
- ✅ الشكاوى → Firebase + localStorage (caching)
- ✅ المستندات/الصور → Firebase + localStorage (caching)
- ✅ بيانات المشرفين → Firebase + localStorage (caching)
- ❌ كلمات المرور → Firebase Authentication فقط (مُشفرة)

**لماذا هذا النهج آمن:**
1. Firebase هو المصدر الرئيسي للحقيقة
2. localStorage فقط لتسريع العرض
3. عند فتح التطبيق → يُحمل من Firebase
4. عند التعديل → يُحفظ في Firebase ثم localStorage

### للمستخدمين:
- استخدم كلمة مرور قوية
- لا تشارك بيانات الدخول
- سجّل الخروج بعد الانتهاء
- حدّث التطبيق باستمرار

## التحديثات الأمنية

التطبيق يتحقق تلقائياً من التحديثات عند التشغيل.
